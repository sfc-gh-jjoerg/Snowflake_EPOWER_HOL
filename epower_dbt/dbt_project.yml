name: epower_analytics
version: '1.0.0'

profile: default

model-paths: ["models"]
macro-paths: ["macros"]
seed-paths: ["seeds"]

seeds:
  epower_analytics:
    +schema: EPOWER_GOLD

models:
  epower_analytics:
    epulse_vpp:
      staging:
        +schema: EPOWER_SILVER
        +materialized: table
    business:
      semantic:
        +schema: EPOWER_GOLD
        +tags: ['semantic']      marts:
        +schema: EPOWER_GOLD
        +materialized: table
      presentation:
        +schema: EPOWER_GOLD
        +materialized: view
      semantic:
        +schema: EPOWER_GOLD
        +tags: ['semantic']
    energy_market_data:
      staging:
        +schema: EPOWER_SILVER
        +materialized: incremental
      marts:
        +schema: EPOWER_GOLD
        +materialized: table
